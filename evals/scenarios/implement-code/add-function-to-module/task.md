# 과제

`textutils.py`에 `slugify` 함수를 추가 구현하세요. 기존 `shout` 함수의 동작은
변경하지 마세요. `test_textutils.py`의 모든 테스트(기존 + 신규)가 통과해야
합니다. 작업 완료 후 표준 DELEGATION_SIGNAL 형식(`---DELEGATION_SIGNAL---` /
`TYPE:` / `---END_SIGNAL---`)으로 결과를 보고하세요.
