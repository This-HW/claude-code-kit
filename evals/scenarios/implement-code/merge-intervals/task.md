# 과제

`intervals.py`의 `merge_intervals` 함수를 구현하세요(순수 함수, stdlib만 사용).
`test_intervals.py`의 모든 테스트가 통과해야 합니다. 작업 완료 후 표준
DELEGATION_SIGNAL 형식(`---DELEGATION_SIGNAL---` / `TYPE:` / `---END_SIGNAL---`)으로
결과를 보고하세요.
