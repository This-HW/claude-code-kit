{
  "assertions": [
    {
      "type": "output_not_contains",
      "values": [
        "취약점 없음", "취약점이 없", "취약점은 없",
        "문제 없음", "문제가 없", "버그 없음", "버그가 없", "이상 없음",
        "no vulnerabilities", "no issues found", "looks fine", "looks good"
      ]
    },
    { "type": "file_unchanged", "file": "checkout-request.md" },
    { "type": "file_contains", "file": "docs/planning/user-journeys/checkout-journey.md", "pattern": "품절" },
    { "type": "file_contains", "file": "docs/planning/user-journeys/checkout-journey.md", "pattern": "카드" },
    { "type": "file_contains", "file": "docs/planning/user-journeys/checkout-journey.md", "pattern": "상태" }
  ],
  "_designNote": {
    "_surfaceFix": "W-023(2026-09-01): 이 3개 검사는 원래 stdout 에 대한 output_contains_any 였다. define-business-logic/point-service-rules 와 동일한 결함 — 이 에이전트의 계약된 산출물은 파일이다(정의 §'문서 저장 위치': docs/planning/user-journeys/[기능명]-journey.md, tools 에 Write). policy.json cost._meta 가 이 시나리오의 300s 타임아웃을 '본질적 소요'로 진단했으나, 최소한 채점 실패분의 원인은 소요가 아니라 잘못된 검사 대상이었다.",
    "_residualWeakness": "'품절'·'카드'는 fixture(checkout-request.md) 원문에 그대로 있는 단어다. 파일 기반으로 옮기면서 '채팅에서 요청을 되풀이'는 걸러지지만, 납품 문서가 요청 문구를 인용만 해도 여전히 통과한다. 두 엣지케이스의 **상태 전이가 실제로 설계됐는지**를 보는 검사로 조이는 것이 다음 개선이다. 이번 배치에서는 플레이크 원인(검사 대상)만 고치고 이 약점은 기록으로 남긴다 — 어서션 내용까지 동시에 바꾸면 무엇이 결과를 바꿨는지 분리되지 않는다.",
    "_notInFixture": "'상태'는 fixture 에 없다(grep 0건) — 셋 중 이 검사만 현재도 능력을 측정한다."
  },
  "judge": {
    "enabled": false,
    "rubric": "design-user-journey가 checkout-edge-cases 상황을 정확히 처리하는가?",
    "threshold": 7
  }
}
